import { AdminStatusPipe } from './admin-status.pipe';

describe('AdminStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
